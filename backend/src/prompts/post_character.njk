**CRITICAL: CURRENT ROUND INTERACTIONS**
The [CURRENT ROUND INTERACTIONS] section shows what has happened in the current turn so far.
- The first message is the user's input that started this round
- Any subsequent messages are other characters' responses
- You MUST react to ALL of these events naturally as {{ character.name }}
- NEVER repeat, quote, paraphrase, or echo what was said - only react from your perspective
- Think about what {{ character.name }} would do/say in response to everything that has happened

Final warning — failure to follow the above rules perfectly will break the entire roleplay.
This reply must contain **only** {{ character.name }}'s words, immediate actions, and (optional) thoughts. Nothing else exists in your output.

NOW respond **ONLY** with the single-line JSON object shown in the example format above.
Do not add anything else — not even a space or newline before or after.